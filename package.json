{
  "name": "joshpaulpopkin",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "bash scripts/publish-pages.sh",
    "deploy:worker": "node build.js && wrangler deploy",
    "dev": "node build.js && wrangler dev --host www.joshpaulpopkin.com --port 8797"
  },
  "dependencies": {
    "image-size": "^2.0.4",
    "marked": "^18.0.13"
  }
}